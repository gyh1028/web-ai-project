<!--定义Mapper映射文件的约束和基本结构-->
<!--映射文件必须保持和mapper类同包同名，且不要直接创建目录文件com.ahu.mapper，结果是一级文件夹-->
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ahu.mapper.StudentMapper">
    <select id="list" resultType="com.ahu.pojo.Student">
        select s.*, c.name clazzName
        from student as s left join clazz as c on s.clazz_id = c.id
        <where>
            <if test="name != null and name != ''">
                s.name like concat('%',#{name},'%')
            </if>
            <if test="degree != null and degree != ''">
                and s.degree = #{degree}
            </if>
            <if test="clazzId != null and clazzId != ''">
                and s.clazz_id = #{clazzId}
            </if>
        </where>
    </select>

    <delete id="deleteByIds">
        delete from student as s
        <where>
            id in <foreach collection="ids" item="id" open="(" close=")" separator=",">
                #{id}
            </foreach>
        </where>
    </delete>

    <insert id="insert">
        insert into student(name, no, gender, phone, degree, clazz_id, id_card, is_college, address, graduation_date, create_time, update_time, violation_count, violation_score)
        values(#{name}, #{no}, #{gender}, #{phone}, #{degree}, #{clazzId}, #{idCard}, #{isCollege}, #{address}, #{graduationDate}, #{createTime}, #{updateTime}, #{violationCount}, #{violationScore})
    </insert>

    <select id="getInfo" resultType="com.ahu.pojo.Student">
        select * from student where id = #{id}
    </select>

    <update id="update">
        update student
        <set>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="no != null and no != ''">no = #{no},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="phone != null and phone != ''">phone = #{phone},</if>
            <if test="degree != null">degree = #{degree},</if>
            <if test="clazzId != null">clazz_id = #{clazzId},</if>
            <if test="idCard != null and idCard != ''">id_card = #{idCard},</if>
            <if test="isCollege != null">is_college = #{isCollege},</if>
            <if test="address != null and address != ''">address = #{address},</if>
            <if test="graduationDate != null">graduation_date = #{graduationDate},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        where id = #{id}
    </update>

    <update id="violation">
        update student
        <set>
            violation_count = violation_count + 1,
            violation_score = violation_score + #{score},
        </set>
        where id = #{id}
    </update>

    <!--统计各个学历的学员人数-->
    <select id="countStudentDegreeData" resultType="java.util.Map">
        select
            (case degree when 1 then '初中'
                         when 2 then '高中'
                         when 3 then '大专'
                         when 4 then '本科'
                         when 5 then '硕士'
                         when 6 then '博士'
                         else '其他' end) name,
            count(*) as value
        from student group by degree ;
    </select>

    <!--统计各个班级的学员人数-->
    <select id="countStudentCountData" resultType="java.util.Map">
        select
            (case clazz_id when 1 then 'JavaEE就业163期'
                           when 2 then '前端就业90期'
                           when 3 then 'JavaEE就业165期'
                           when 4 then 'JavaEE就业166期'
                           when 5 then '大数据就业58期'
                           when 6 then 'JavaEE就业167期'
                           else '其他' end)  clazz,
            count(*) total
        from student group by clazz_id
        order by total
    </select>
</mapper>